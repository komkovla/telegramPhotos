# ── Telegram ─────────────────────────────────────────────────────────
# Bot token from @BotFather (required)
TELEGRAM_BOT_TOKEN=

# Local Bot API server credentials — required for Docker default setup.
# Obtain from https://my.telegram.org → API development tools.
TELEGRAM_API_ID=
TELEGRAM_API_HASH=

# Bot API base URL.
# Unset or omit        → use local server at http://telegram-bot-api:8081 (Docker default)
# Empty value (=)      → use official api.telegram.org only (20 MB file limit)
# http://localhost:8081 → local dev: bot on host, API server in Docker
# TELEGRAM_BOT_API_URL=

# ── Google Photos ────────────────────────────────────────────────────
# OAuth 2.0 credentials from Google Cloud Console (required)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Refresh token obtained via: python scripts/obtain_token.py (required)
GOOGLE_REFRESH_TOKEN=

# ── Optional ─────────────────────────────────────────────────────────
# Restrict syncing to specific groups (comma-separated chat IDs, e.g. -100123,-100456).
# Leave empty to sync all groups the bot is in.
ALLOWED_GROUP_IDS=

# SQLite database path (default: /data/bot.db)
DB_PATH=/data/bot.db

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO
